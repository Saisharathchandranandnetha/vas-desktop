appId: com.vas.desktop
productName: VAS Desktop
copyright: Copyright © 2026 VAS Team

directories:
  output: release
  buildResources: build

files:
  - dist/**/*
  - package.json
  - "!**/*.{ts,map}"
  - from: "../../packages/shared"
    to: "node_modules/@vas/shared"
    filter:
      - "dist/**/*"
      - "package.json"
  - from: "../../packages/database"
    to: "node_modules/@vas/database"
    filter:
      - "dist/**/*"
      - "package.json"
  - from: "../../packages/provider-sdk"
    to: "node_modules/@vas/provider-sdk"
    filter:
      - "dist/**/*"
      - "package.json"
  - from: "../../packages/agent-sdk"
    to: "node_modules/@vas/agent-sdk"
    filter:
      - "dist/**/*"
      - "package.json"
  - from: "../../packages/mcp-sdk"
    to: "node_modules/@vas/mcp-sdk"
    filter:
      - "dist/**/*"
      - "package.json"

asar: true
compression: maximum

generateUpdatesFilesForAllChannels: true

publish:
  provider: github
  owner: vas-team
  repo: vas-desktop
  releaseType: draft

win:
  target:
    - target: nsis
      arch:
        - x64
        - arm64
  icon: build/icon.ico
  artifactName: "${productName}-${version}-${arch}.${ext}"

nsis:
  oneClick: true
  perMachine: false
  deleteAppDataOnUninstall: false
  allowToChangeInstallationDirectory: false
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  installerHeaderIcon: build/icon.ico

mac:
  target:
    - target: dmg
      arch:
        - x64
        - arm64
  icon: build/icon.icns
  category: public.app-category.developer-tools
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  artifactName: "${productName}-${version}-${arch}.${ext}"

dmg:
  contents:
    - x: 130
      y: 220
    - x: 410
      y: 220
      type: link
      path: /Applications

linux:
  target:
    - target: AppImage
      arch:
        - x64
        - arm64
    - target: deb
      arch:
        - x64
  icon: build/icons
  category: Development
  artifactName: "${productName}-${version}-${arch}.${ext}"
